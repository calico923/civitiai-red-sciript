# Copy this file to `.env` and set your token (Civiti.red / Civitai account settings).
# scrape_newest_ratio.py loads `.env` by default; override with `--env-file /path/to/file`.
CIVITAI_API_KEY=your_token_here
